name: hermes-lansenger-adapter
version: 2.6.0
description: Lansenger (蓝信) platform adapter + messaging tools for Hermes Agent — WebSocket gateway, text/markdown/file/image/linkCard sending, message revocation
manifest_version: 1
kind: standalone
provides:
  - platforms/lansenger
  - lansenger-tools